const $ = id => document.getElementById(id);
let analysis = null;
let selectedFile = null;
let progressTimer = null;
let objectUrl = null;

const instrumentLabels = { bass5: "Baixo", guitar: "Guitarra", piano: "Piano / Teclado" };

function toast(message){const el=$("toast");el.textContent=message;el.classList.add("show");clearTimeout(el._timer);el._timer=setTimeout(()=>el.classList.remove("show"),2400);}
function setStatus(message,type=""){const el=$("status");el.textContent=message;el.className="status"+(type?` ${type}`:"");el.classList.remove("hidden");}
function setExportStatus(message,type=""){const el=$("exportStatus");el.textContent=message;el.className="status"+(type?` ${type}`:"");el.classList.remove("hidden");}
function formatBytes(bytes){if(bytes<1024)return `${bytes} B`;if(bytes<1024*1024)return `${(bytes/1024).toFixed(1)} KB`;return `${(bytes/1024/1024).toFixed(1)} MB`;}

function selectFile(file){if(!file)return;selectedFile=file;$("fileTitle").textContent=file.name;$("fname").textContent=`${formatBytes(file.size)} · pronto para analisar`;$("audioName").textContent=file.name;$("audioSize").textContent=formatBytes(file.size);if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=URL.createObjectURL(file);$("player").src=objectUrl;$("audioPreview").classList.remove("hidden");$("status").classList.add("hidden");}

$("chooseFile").onclick=e=>{e.stopPropagation();$("audio").click();};
$("dropzone").onclick=()=>$("audio").click();
$("dropzone").onkeydown=e=>{if(e.key==="Enter"||e.key===" "){$("audio").click();e.preventDefault();}};
$("audio").onchange=()=>selectFile($("audio").files[0]);
["dragenter","dragover"].forEach(evt=>$("dropzone").addEventListener(evt,e=>{e.preventDefault();$("dropzone").classList.add("drag");}));
["dragleave","drop"].forEach(evt=>$("dropzone").addEventListener(evt,e=>{e.preventDefault();$("dropzone").classList.remove("drag");}));
$("dropzone").addEventListener("drop",e=>selectFile(e.dataTransfer.files[0]));

function startProgress(){clearInterval(progressTimer);$("progressBox").classList.remove("hidden");let value=5;const stages=[[12,"A preparar o áudio…"],[28,"A detetar BPM…"],[45,"A estimar a tonalidade…"],[65,"A analisar os acordes…"],[82,"A organizar a estrutura…"],[94,"A finalizar resultados…"]];let stage=0;const update=()=>{if(stage<stages.length&&value>=stages[stage][0]){$("progressText").textContent=stages[stage][1];stage++;}$("progressPercent").textContent=`${Math.round(value)}%`;$("progressBar").style.width=`${value}%`;value=Math.min(94,value+Math.max(1,(94-value)*.065));};update();progressTimer=setInterval(update,550);}
function finishProgress(){clearInterval(progressTimer);$("progressText").textContent="Análise concluída";$("progressPercent").textContent="100%";$("progressBar").style.width="100%";setTimeout(()=>$("progressBox").classList.add("hidden"),700);}
function failProgress(){clearInterval(progressTimer);$("progressBox").classList.add("hidden");}
function setActiveStep(number){document.querySelectorAll(".step").forEach((s,i)=>s.classList.toggle("active",i===number-1));}
document.querySelectorAll(".step").forEach(btn=>btn.onclick=()=>{const target=$(btn.dataset.target);if(target&&!target.classList.contains("hidden"))target.scrollIntoView({behavior:"smooth",block:"start"});});

$("go").onclick=async()=>{if(!selectedFile){setStatus("Escolhe primeiro uma música.","error");return;}$("go").disabled=true;setStatus("A análise está em curso. O tempo depende da duração da música.");startProgress();const fd=new FormData();fd.append("file",selectedFile);fd.append("instrument",$("instrument").value);try{const response=await fetch("/api/analyze",{method:"POST",body:fd});const raw=await response.text();let data;try{data=JSON.parse(raw);}catch{throw new Error(`Resposta inválida do servidor: ${raw.substring(0,160)}`);}if(!response.ok||!data.ok)throw new Error((data.error||"Erro na análise")+(data.detail?` ${data.detail}`:""));analysis=data;populateReview();finishProgress();setStatus("Análise concluída. Revê agora os resultados.","success");$("reviewCard").classList.remove("hidden");$("exportCard").classList.remove("hidden");setActiveStep(2);setTimeout(()=>$("reviewCard").scrollIntoView({behavior:"smooth",block:"start"}),250);}catch(error){failProgress();setStatus(`Erro: ${error.message}`,"error");}finally{$("go").disabled=false;}};

function populateReview(){$("title").value=analysis.title||selectedFile?.name.replace(/\.[^.]+$/,")")||"Música";$("bpmInput").value=analysis.tempo||120;$("keyInput").value=analysis.key||"C";$("meterInput").value=analysis.meter||"4/4";$("durationValue").textContent=formatDuration(analysis.duration||0);$("barsValue").textContent=analysis.bars_analyzed||countBars();$("instrumentValue").textContent=instrumentLabels[analysis.instrument]||"Instrumento";$("signal").textContent=analysis.analysis_signal||"-";$("notes").innerHTML=(analysis.detected_notes||[]).map(n=>`<span>${escapeHtml(n)}</span>`).join("")||"<span>Sem notas estáveis</span>";renderSections();}
function formatDuration(seconds){const min=Math.floor(seconds/60),sec=Math.round(seconds%60);return `${min}:${String(sec).padStart(2,"0")}`;}
function countBars(){return (analysis?.sections||[]).reduce((sum,s)=>sum+(s.chords?.length||0),0);}
function escapeHtml(value){return String(value??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");}
function syncMetadata(){if(!analysis)return;analysis.title=$("title").value||"Música";analysis.tempo=Number($("bpmInput").value)||120;analysis.key=$("keyInput").value||"C";analysis.meter=$("meterInput").value||"4/4";$("barsValue").textContent=countBars();}
["title","bpmInput","keyInput","meterInput"].forEach(id=>$(id).addEventListener("input",syncMetadata));

function renderSections(){const box=$("sections");box.innerHTML="";(analysis.sections||[]).forEach((section,index)=>{const el=document.createElement("article");el.className="section-card";el.innerHTML=`<div class="section-card-header"><div class="section-index">${String(index+1).padStart(2,"0")}</div><input class="section-name" data-section-name="${index}" value="${escapeHtml(section.name||`SECÇÃO ${index+1}`)}" aria-label="Nome da secção"><label class="repeat-wrap"><span>Repetições</span><input data-repeat="${index}" type="number" min="1" value="${section.repeat||1}"></label><div class="section-menu"><button class="icon-btn" data-up="${index}" title="Subir secção">↑</button><button class="icon-btn" data-down="${index}" title="Descer secção">↓</button><button class="icon-btn" data-copy="${index}" title="Duplicar secção">⧉</button><button class="icon-btn danger" data-remove="${index}" title="Remover secção">×</button></div></div><div class="chords-label"><span>Acordes por compasso</span><button class="text-btn" data-add-chord="${index}">+ Adicionar compasso</button></div><div class="chords-grid">${(section.chords||[]).map((chord,chordIndex)=>`<div class="chord-cell"><input data-chord-section="${index}" data-chord-index="${chordIndex}" value="${escapeHtml(chord)}" aria-label="Acorde do compasso ${chordIndex+1}"><button data-remove-chord="${index}:${chordIndex}" title="Remover compasso">×</button></div>`).join("")}</div>`;box.appendChild(el);});box.querySelectorAll("[data-section-name]").forEach(el=>el.oninput=()=>analysis.sections[+el.dataset.sectionName].name=el.value);box.querySelectorAll("[data-repeat]").forEach(el=>el.oninput=()=>analysis.sections[+el.dataset.repeat].repeat=Math.max(1,+el.value||1));box.querySelectorAll("[data-chord-section]").forEach(el=>el.oninput=()=>analysis.sections[+el.dataset.chordSection].chords[+el.dataset.chordIndex]=el.value.trim());box.querySelectorAll("[data-add-chord]").forEach(el=>el.onclick=()=>{analysis.sections[+el.dataset.addChord].chords.push("C");renderSections();syncMetadata();});box.querySelectorAll("[data-remove-chord]").forEach(el=>el.onclick=()=>{const [s,c]=el.dataset.removeChord.split(":").map(Number);analysis.sections[s].chords.splice(c,1);renderSections();syncMetadata();});box.querySelectorAll("[data-remove]").forEach(el=>el.onclick=()=>{analysis.sections.splice(+el.dataset.remove,1);renderSections();syncMetadata();});box.querySelectorAll("[data-copy]").forEach(el=>el.onclick=()=>{const i=+el.dataset.copy;analysis.sections.splice(i+1,0,JSON.parse(JSON.stringify(analysis.sections[i])));renderSections();syncMetadata();});box.querySelectorAll("[data-up]").forEach(el=>el.onclick=()=>moveSection(+el.dataset.up,-1));box.querySelectorAll("[data-down]").forEach(el=>el.onclick=()=>moveSection(+el.dataset.down,1));}
function moveSection(index,direction){const target=index+direction;if(target<0||target>=analysis.sections.length)return;[analysis.sections[index],analysis.sections[target]]=[analysis.sections[target],analysis.sections[index]];renderSections();}
$("addSection").onclick=()=>{analysis.sections.push({name:"NOVA SECÇÃO",repeat:1,chords:["C","G","Am","F"]});renderSections();syncMetadata();};
$("goExport").onclick=()=>{syncMetadata();setActiveStep(3);$("exportCard").scrollIntoView({behavior:"smooth",block:"start"});};

function payload(kind){syncMetadata();return {kind,title:analysis.title,instrument:analysis.instrument,tempo:analysis.tempo,key:analysis.key,meter:analysis.meter,sections:analysis.sections};}
async function exportPdf(kind){if(!analysis)return;const btn=kind==="score"?$("pdfScore"):$("pdfChart");btn.disabled=true;setExportStatus("A criar o PDF…");try{const response=await fetch("/api/export",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload(kind))});if(!response.ok){const raw=await response.text();let msg=raw;try{const data=JSON.parse(raw);msg=(data.error||"Erro")+(data.detail?` ${data.detail}`:"");}catch{}throw new Error(msg);}const blob=await response.blob();const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=(kind==="score"?"Pauta_":"Partitura_Acordes_")+(analysis.title||"Música")+".pdf";document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),2000);setExportStatus("PDF criado com sucesso.","success");toast("PDF pronto");}catch(error){setExportStatus(`Erro: ${error.message}`,"error");}finally{btn.disabled=false;}}
$("pdfScore").onclick=()=>exportPdf("score");
$("pdfChart").onclick=()=>exportPdf("chart");
